# 基于大语言模型的语音驱动数字电路设计与自动化仿真系统报告

**课程名称**：大语言模型应用开发

**学生姓名**：蔡成易

**日期**：2025年12月30日

---

## 1. 项目背景与目标

在传统的电子设计自动化（EDA）流程中，硬件工程师需要手动编写复杂的 Verilog 代码或在 GUI 软件中繁琐地摆放元器件。随着大语言模型（LLM）的兴起，自然语言驱动的软硬件协同设计成为可能。

本项目旨在构建一个**端到端、多模态的智能电路辅助设计系统**。通过整合 DeepSeek-V3 的自然语言处理能力、Google 语音识别技术以及 Verilator 开源仿真工具链，实现从“口述需求”到“生成电路”再到“自动仿真验证”的全自动化闭环。

---

## 2. 系统架构设计

系统采用模块化架构，将任务分解为输入处理、逻辑推理、代码/文件生成及物理仿真四个阶段。

### 2.1 核心流程图

1. **多模态输入**：支持文本输入与基于中文语音识别的指令输入。
2. **LLM 逻辑转换**：根据用户选择的模式（Logisim 模式或 Verilog 模式），调用特定的 System Prompt 引导 LLM 输出结构化 JSON。
3. **解析与渲染**：
* **Logisim 模式**：将 JSON 组件列表转换为符合 `.circ` 规范的 XML。
* **Verilog 模式**：提取 HDL 代码，并驱动 LLM 生成对应的 C++ Testbench。


4. **自动化仿真流**：调用 Verilator 编译器进行硬件模拟，生成 FST 波形文件供工程师查看。

---

## 3. 关键技术实现

### 3.1 针对 EDA 的提示词工程 (Prompt Engineering)

为了保证 LLM 生成内容的稳定性，本项目设计了三套高度专业化的 System Prompt：

* **数字逻辑架构师 (Logisim)**：强调库（lib）、坐标（stage）和网络（net）的对应关系，特别是针对 2 输入门电路的引脚偏移逻辑。
* **FPGA 工程师 (Verilog)**：强制要求输出 IEEE 1364 标准代码，并以 JSON 格式封装以方便 Python 解析。
* **验证工程师 (C++ Testbench)**：通过上下文注入 Verilog 模块名，并严格约束 LLM 不得“幻觉”出代码中不存在的端口（如 clk 或 rst）。

### 3.2 几何布局算法优化

在 Logisim 电路生成过程中，LLM 通常难以处理精确的图形位置。本项目在 Python 后端实现了一套**自动布局修正逻辑**：

* **坐标计算**：根据组件所在的 `stage`（阶段）自动计算 X 轴，根据组件数量自动计算 Y 轴偏置。
* **引脚对齐**：通过逻辑修正（Line 98-124），对 2 输入和多输入逻辑门的引脚位置进行了  和  的几何补偿，确保生成的 Tunnel（隧道）与 Gate（逻辑门）引脚完美重合。

### 3.3 Verilator 自动化验证闭环

系统不仅生成代码，还解决了“最后一公里”的验证问题：

1. **动态头文件生成**：通过正则提取 Verilog 模块名，自动构造 `V[module_name].h` 的引用。
2. **仿真驱动生成**：利用 LLM 自动识别模块输入，编写驱动激励。
3. **波形追踪**：集成 `verilated_fst_c.h`，确保仿真运行后产生 `.fst` 文件，支持使用 GTKWave 进行时序分析。

---

## 4. 代码模块解析

### 4.1 核心组件表

| 模块名称                  | 技术栈                | 功能描述 |
|        ---               |          ---         | --- |
| `listen_command`         | `speech_recognition` | 环境噪音自适应校准，支持中文长语音采集 |
| `query_deepseek`         | `requests` / JSON    | 支持多模式切换（Circuit/Verilog/C++ TB）的 API 封装 |
| `generate_circuit_file`  | Python XML 处理       | 针对 Logisim-evolution 3.8.0 的 XML 模板注入 |
| `run_verilator_sim`      | `subprocess`         | 自动清理环境、调用 `verilator` 编译并执行仿真 |

### 4.2 鲁棒性设计

代码中包含了大量的异常处理（Try-Except）以及编译错误捕获逻辑，确保当 AI 生成的代码出现语法错误时，用户能清晰地看到控制台反馈而非系统崩溃。

---

## 5. 项目演示与实验结果

运行环境：Ubuntu 24.04，Logisim-evolution v3.8.0，Verilator 5.0+
运行命令 source venv/bin/activate
        python voice_circuit.py
        deactivate


### 5.1 场景一：可视电路设计

**用户指令**：“帮我做一个由两个异或门组成的全加器。”
**系统反馈**：生成 `voice_circuit_v24_perfect.circ`。
**结果**：电路图中 XOR 门布局整齐，所有 Tunnel 连接正确，可在 Logisim 中通过 Poke Tool 直接进行功能验证。

### 5.2 场景二：代码生成与仿真

**用户指令**：“生成一个 8 位带异步复位的移位寄存器。”
**系统反馈**：

1. 生成符合规范的 Verilog 代码。
2. 自动识别 `clk` 和 `rst` 端口，并生成对应的 C++ 翻转逻辑。
3. 自动调用 Verilator，终端显示 `✅ 仿真完成！` 并产生 `wave.fst`。

---

## 6. 总结与展望

### 6.1 项目总结

本项目展示了大语言模型在辅助硬件设计方面的巨大潜力。通过将 LLM 的灵活性与 EDA 软件的严谨性结合，极大地降低了数字电路设计的门槛，缩短了从创意到原型验证的周期。

### 6.2 未来方向

* **错误反馈迭代**：当 Verilator 编译报错时，将报错信息自动喂回给 LLM 进行代码自修复（Auto-Debugging）。
* **支持更多组件**：扩展对多位总线（Bus）、RAM 及更复杂时序逻辑组件的图形化支持。
* **本地化部署**：集成大模型本地推演框架，提升语音响应的实时性。

---

> **附录**：本项目代码已在 Ubuntu 24.04 系统环境下完成测试，兼容 Logisim-evolution v3.8.0 与 Verilator 5.0+。

